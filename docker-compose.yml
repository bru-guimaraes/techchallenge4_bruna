services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: lstm-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
